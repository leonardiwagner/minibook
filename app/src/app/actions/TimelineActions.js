import alt from '../alt';

class TimelineActions {
    loadTimeline() {
        return 'wat';
    }

    writeEvent(event){
        return event;
    }
}

export default alt.createActions(TimelineActions);